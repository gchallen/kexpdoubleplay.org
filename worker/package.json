{
  "name": "kexpdoubleplays-worker",
  "version": "2026.3.0",
  "private": true,
  "scripts": {
    "test": "bun test",
    "dev": "wrangler dev",
    "deploy": "cd ../types && bun run build && cd ../worker && wrangler deploy && curl -s -H 'Title: Worker Deployed' -H 'Tags: rocket' -d \"kexpdoubleplays-api deployed at $(date -u '+%Y-%m-%d %H:%M UTC')\" ntfy.sh/kexpdoubleplays-f0b346e8",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@kexp-doubleplay/types": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260317.1",
    "typescript": "5.9.3",
    "wrangler": "4.76.0"
  }
}
